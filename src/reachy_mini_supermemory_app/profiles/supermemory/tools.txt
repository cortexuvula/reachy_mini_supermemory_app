dance
stop_dance
play_emotion
stop_emotion
camera
idle_do_nothing
head_tracking
move_head
save_memory
recall_memory
manage_memory
web_search
# task_status and task_cancel are auto-injected by upstream's loader
# (tools/core_tools.py:188 — SystemTool enum) so they don't need to be listed
# here. The instructions.txt "Background tasks" section still teaches the
# model when to use them.
