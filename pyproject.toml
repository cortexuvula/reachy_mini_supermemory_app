[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "reachy_mini_supermemory_app"
version = "0.1.0"
authors = [{ name = "Andre Hugo" }]
description = "Reachy Mini conversation app with long-term memory backed by supermemory.ai"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
  # Pulled from main; the runtime monkey-patches in _patches.py + _privacy_mode.py
  # reach into specific upstream symbols (ServerVad, LOCKED_PROFILE,
  # get_session_instructions, PersonalityUI._list_personalities,
  # MovementManager._issue_control_command). When upstream releases a refactor,
  # apply_all_patches() emits a "patches applied: N/M" roll-up at startup so
  # operators see which ones survived the version bump. If you pin this, also
  # update the supported-upstream commit in the README.
  "reachy_mini_conversation_app @ git+https://github.com/pollen-robotics/reachy_mini_conversation_app",
  "httpx>=0.27",
]

[dependency-groups]
dev = [
  "pytest",
  "pytest-asyncio",
  "respx>=0.21",
  "ruff==0.12.0",
  "mypy==1.18.2",
]

[project.scripts]
reachy-mini-supermemory-app = "reachy_mini_supermemory_app.main:main"

[project.entry-points."reachy_mini_apps"]
reachy_mini_supermemory_app = "reachy_mini_supermemory_app.main:ReachyMiniSupermemoryApp"

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
reachy_mini_supermemory_app = ["static/*", "profiles/**/*.txt", "profiles/**/*.py"]

[tool.ruff]
line-length = 119
exclude = [".venv", "dist", "build", "**/__pycache__", "*.egg-info"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "C4", "D"]
ignore = ["E501", "D100", "D203", "D213"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["D"]

[tool.mypy]
python_version = "3.12"
files = ["src/"]
ignore_missing_imports = true
strict = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
